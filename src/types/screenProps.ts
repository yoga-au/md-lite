import type {NativeStackScreenProps} from '@react-navigation/native-stack';

type NativeStackParamsList = {
  Latest: undefined;
};

export type ScreenProps = NativeStackScreenProps<NativeStackParamsList>;
